# GitHub Pages deployment disabled - frontend is now deployed via Vercel
# Keeping this file for reference only

# name: Deploy to GitHub Pages
# on:
#   push:
#     branches: [main]
#     paths:
#       - 'client/**'
